# ===================================
# UI .env — baked into React build
# ===================================
# Copy this file to .env and fill in your values

# Supabase (client-side, safe to expose — anon key only)
REACT_APP_SUPABASE_URL=https://xxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJ...

# Google OAuth
REACT_APP_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com

# Admin email (used for client-side UI gating only; server validates separately)
REACT_APP_ADMIN_EMAIL=you@example.com

# Push notifications (public key only — private key stays server-side)
REACT_APP_VAPID_PUBLIC_KEY=BFVu...

# Google Analytics (optional)
REACT_APP_GA_MEASUREMENT_ID=

# Engine mode — always "live" for production
REACT_APP_ENGINE_MODE=live

# ===================================
# Vercel Environment Variables
# ===================================
# These must also be set in Vercel dashboard for production:
#
# SUPABASE_URL             — Supabase project URL
# SUPABASE_SERVICE_ROLE_KEY — SECRET, backend-only
# CRON_SECRET              — SECRET, bearer token for cron endpoints
# ADMIN_EMAIL              — Admin email for server-side validation
# GOOGLE_CLIENT_ID         — Google OAuth client ID (server-side)
# VAPID_PUBLIC_KEY         — Web Push public key
# VAPID_PRIVATE_KEY        — SECRET, Web Push private key
# VAPID_SUBJECT            — mailto: or https:// for VAPID
